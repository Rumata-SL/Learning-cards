import React from 'react'

import styles from './Card.module.css'

type PackPropsType = {}

export const Card: React.FC<PackPropsType> = (props: PackPropsType) => {
  return <div>Страница карточки</div>
}
