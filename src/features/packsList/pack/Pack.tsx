import React from "react";
import s from "./Pack.module.css"

type PackPropsType = {}

export const Pack: React.FC<PackPropsType> = (props) => {
    return (
        <div>
            Страница колоды карточек
        </div>
    );
};